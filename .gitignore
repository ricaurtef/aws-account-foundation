terraform.tfvars
.terraform/*
terraform.tfstate*

# Local .terraform directories
**/.terraform/*

# Terraform lock file (not committed for reusable modules — but this is a root config, so include it)
# .terraform.lock.hcl

# Claude Code
CLAUDE.md
